// biospy MCP server — JSON-RPC 2.0 over stdio. D19 implements the protocol + 18 tools.

fn main() -> anyhow::Result<()> {
    eprintln!("biospy-mcp {} — D19 stub", env!("CARGO_PKG_VERSION"));
    Ok(())
}
